  /* nav {
    display: flex;
    min-height: 200px;
    /* Needed to position the navbar */
    position: relative;
    background: linear-gradient(375deg, red, maroon);
  } */

  /* footer {
    display: flex;
    min-height: 300px;
    /* Needed to position the navbar */
    position: relative;
    background: linear-gradient(375deg, red, maroon);
  } */